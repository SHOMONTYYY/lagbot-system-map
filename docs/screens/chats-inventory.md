# Chats + Inventory + Product editor/import — code-true component inventory
> Generated from app code @ aef6e84 (2026-06-11).

## CHATS — conversation list
1. Header — eyebrow "CONVERSATIONS" (orange tracked) · title "Chats" · Search icon (toggles search bar) · status chip "Connect WhatsApp" OR "Lagbot active/paused" (dot + chevron, → WhatsApp setup) · stat pills "[N] today | [N] unread | [N] leads · 7d" (orange when >0)
2. Search bar (conditional) — "Search conversations or messages", X clear, closes on blur
3. Tag filter pills — All · Lead · Quoted · Paid · Archive (unique hue ring when active; pill SHAKES when that tag has a conversation awaiting reply)
4. Segmented control — All | Unread | AI Handled (count badges, orange active)
5. Sections: "Needs you" (orange label, count pill, = manual status OR unread>0) · "AI handling" (dark label)
6. ConversationRow — left edge stroke 4px (orange waiting / green responded / grey idle), avatar 44px, name + timestamp, pills row (TagChip + "Responded"), 2-line preview + unread badge ("9+"), pulse animation when last message inbound. SWIPE RIGHT → Archive/Restore (PATCH /api/conversations/:id/archive). Tap → thread.
- Empty states: "No matches for [query]" / "No conversations yet — Messages from WhatsApp will appear here" / "No [stage] conversations"

## CHATS — thread view
1. Dark header (matte gradient) — back arrow · avatar + name + status ("You handling" / "Lagbot handling" with dot) — tap → CustomerSummarySheet · pill "AI On" (orange) / "AI Off" — toggles handoff w/ alert, logs human_interventions
2. Messages — DateSeparator chips ("Today"/"Yesterday"/date); inbound = white bubble left 82% + tiny timestamp + optional voice-transcript chip (mic icon, italic); outbound = orange gradient right + "Generated by Lagbot" italic footnote when sender_type='ai'; media pills [Image]/[Voice note]; same-sender messages tuck tight
3. SmartReplyStrip (only manual mode + inbound last) — "✦ LAGBOT SUGGESTS" + 3 chips (max 280px) → fills composer, never auto-sends. Data: api.suggestReplies()
4. Composer — "Type a message" multiline + send button (orange when text); send = optimistic bubble, flips conv to manual if AI was on, POST /api/whatsapp/send

## CustomerSummarySheet
"CUSTOMER PROFILE" eyebrow + close · avatar 64px + name + phone + "First seen [time]" · stats row TOTAL SPENT / MESSAGES / FIRST SEEN · details rows Phone/Email/Address (extracted from messages) · HISTORY: per-day timeline cards (orange rail + dots): date + msg count, headline ("Payment confirmed" / "Quote accepted"…), 1-sentence narrative, "Read all messages" → DayDetailModal. Empty: "No conversations yet."

## INVENTORY
1. Hero (dark) — "STOCK" eyebrow · "Inventory" title · 3 icon buttons: Upload (→ /(product)/import), Plus (→ /(product)/new), Search (toggle) · stats TOTAL | LOW STOCK (orange >0) | VALUE (₦ K/M)
2. BestSellerCard (only if confirmed sales this month) — Award icon, "BEST SELLER", product name + price
3. Search bar "Search products..." (conditional)
4. Category pills (only if >1 category) — All + categories
5. Sections: "NEEDS REORDER" (orange pulsing) · "IN STOCK"
6. ProductListItem — image/Package icon in brand frame, category chip, name, ₦ price, stock number color-coded (green/orange/red) + dot + "In stock/Low stock/Out of stock". Tap → ProductDetailSheet
- Empty: "No products yet — Tap the + button to add your first product." / search: "No results found"

## ProductDetailSheet (bottom sheet)
Dark header zone w/ 120px image frame · category eyebrow + name + description + "Sizes: …" "Colors: …" lines · stat row Stock/Price/Status · "Edit Product" (dark CTA → /(product)/new?id=) · "Delete product" red link (soft delete active=false)

## PRODUCT EDITOR /(product)/new — opened FROM Inventory (+)
Back (→ replace inventory) · eyebrow "CATALOGUE"/"UPDATE" · title "Add product"/"Edit product" · sub "Fill in the basics, then save to your inventory."
1. ImageUploadCard — 92px gradient tile + upload badge → Alert "Photo upload is coming soon…" (STUBBED)
2. Details card — "Product name" input · Category tiles ×4 (Shoes/Clothing/Accessories/Others)
3. Available Sizes card (OPTIONAL) — chips + "e.g. XS, S, M, L, XL — type and press comma or return"
4. Available Colors card (OPTIONAL) — same pattern
5. Pricing & Stock — "Selling price" ₦ input (min 100) | Stock stepper −/+
6. Description card (OPTIONAL) — "Size, color, material, anything that helps a customer decide"
7. Sticky CTA — "Add Product" / "Save Changes" / "Saving…" → products.insert/update → replace('/(tabs)/inventory')

## IMPORT /(product)/import — opened FROM Inventory (⤴). Phases: pick → preview → importing → done
- PICK: back · "IMPORT" / "Import inventory" / "Drop in a file, pick what to import, save to your catalogue." · "FILE TYPE" · 3 format rows (CSV / Excel / PDF — icon tile + description + example chips) → DocumentPicker → POST /api/products/import · "ℹ Need a template?" → CSV template modal · loading "Parsing file…"
- PREVIEW: "PREVIEW" / "{N} products found" / "Tap rows to deselect…" + "X rows skipped." · Select all/Deselect all · table (checkbox | Name | Price | Cat. | Stock) · errors box "Skipped rows (N): Row X — reason" · CTA "Import N products"
- IMPORTING: spinner + "Importing N of M…" + progress bar
- DONE: green check · "DONE" / "N products imported" / "Your inventory is up to date." (+ red "M couldn't be imported") · "Go to Inventory"

Navigation truths: editor and import are routes pushed FROM Inventory; both return with router.replace('/(tabs)/inventory').
