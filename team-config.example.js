/* Lagbot System Map — shared team keys (TEMPLATE).
   Copy this file to `team-config.js` (gitignored) and fill in the real values.
   index.html loads team-config.js automatically; without it the map still works,
   you just paste keys manually via ☁ Sync and the AI ⚙ settings. */
window.TEAM_CONFIG = {
  /* Supabase team-sync project URL (Project Settings → API → Project URL) */
  supabaseUrl: "https://xxxx.supabase.co",
  /* Supabase "anon public" key (Project Settings → API) — starts with eyJ */
  supabaseAnonKey: "",
  /* Anthropic API key for the ✦ Circuit AI bar — starts with sk-ant- */
  aiKey: "",
};
